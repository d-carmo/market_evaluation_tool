from data.models import TickerReport


def launch_dashboard(reports: list[TickerReport]) -> None:
    """v2 placeholder — UI dashboard not implemented."""
    raise NotImplementedError("UI dashboard is not available in v1.")
