pyarrow>=15.0.0
yfinance>=0.2.40
ccxt>=4.3.0
pandas>=2.2.0
pandas-ta>=0.3.14b
numpy>=1.26.0
scipy>=1.13.0
python-dotenv>=1.0.0
rich>=13.7.0
pytest>=8.0.0
pytest-mock>=3.14.0
# v2 — API + ML + UI
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
xgboost>=2.0.0
scikit-learn>=1.4.0
streamlit>=1.35.0
plotly>=5.22.0
httpx>=0.27.0
apscheduler>=3.10.4
slowapi>=0.1.9
streamlit-autorefresh>=1.0.1
# Optional — Bloomberg Terminal data source (requires Bloomberg subscription)
# blpapi>=3.24.0
# Optional — MySQL database backend (SQLite is used by default)
pymysql>=1.1.0
# v3 — Reddit sentiment + LSTM (Phases 1-4)
praw>=7.7.0                   # Reddit API wrapper
vaderSentiment>=3.3.2         # lightweight NLP sentiment fallback
torch>=2.2.0                  # LSTM predictor (Phase 3); also needed by FinBERT
transformers>=4.38.0          # FinBERT / roBERTa sentiment (higher accuracy than VADER)
